version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_sk
        description: The primary key of the customers generated with row_number
        tests:
          - unique
          - not_null   